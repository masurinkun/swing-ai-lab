# AGENTS.md

このリポジトリは、日本株の自己改善型スイングトレード分析プロジェクトの正本である。Codex は過去のチャット履歴ではなく、このリポジトリ内のファイルを長期記憶として扱う。

## 作業開始時に読む順序

毎回の作業開始時に、必ず以下の順で確認する。

1. `AGENTS.md`
2. `README.md`
3. `rules/current_rules.md`
4. `rules/improvement_history.md`
5. `history/recommendations.csv`
6. `history/evaluations.csv`
7. 直近の `reports/reviews/`
8. 直近の `reports/screening/`

必要に応じて `rules/rule_candidates.md`、`rules/rejected_rules.md`、`history/weekly_performance.csv`、`history/market_environment.csv`、`reports/monthly/`、`reports/experiments/` も確認する。

## 基本原則

- 過去のチャット内容を記憶している前提で作業しない。
- プロジェクト内のファイルを正本とする。
- 最新株価を必ず確認する。
- 古い株価や推測した価格を使用しない。
- 株式分割や併合を確認する。
- 推薦時点で利用できない未来情報を使わない。
- 根拠のない数値を生成しない。
- データが取得できない場合は不明と記録する。
- 候補が弱い週は無理に5銘柄出さない。
- 自動売買や注文執行は行わない。
- 投資助言ではなく分析補助として扱う。
- 1週間の成績だけでルールを恒久変更しない。
- 勝率だけでなく期待値と Profit Factor を重視する。
- 同一セクターへの集中を避ける。
- Git コミット前に変更内容を確認する。
- リモートへの push は明示的に依頼された場合のみ行う。ただし、`prompts/weekly_screening.md` に従う週次スクリーニング自動化は、公開完了までの `main` への push についてユーザーから継続的な明示許可を得ている。
- 公開サイトは閲覧専用とし、Markdown と CSV を正本として静的 HTML を自動生成する。

## 禁止事項

- 証券口座、取引 API、注文執行システムへ接続しない。
- アプリケーションコード、動的 Web サービス、API サーバー、データベースを作成しない。
- ただし、既存の分析記録を読み取り専用で公開する静的 HTML と GitHub Pages 用の生成・配信設定は許可する。
- バックテスト結果や株価を、確認なしに推測で埋めない。
- 評価時に推薦後の未来情報を推薦理由へ混入させない。
- 日足データだけで同日中の Target と Stop の到達順を断定しない。

## 毎週のスクリーニング手順

1. 作業開始時に読む順序のファイルを確認する。
2. 現行ルール、過去成績、直近の失敗原因、検証中の改善仮説を把握する。
3. 最新の市場環境、株価、出来高、移動平均、補助指標、決算、業績予想、適時開示、信用需給、外部環境を調査する。
4. 条件を満たす候補を最大5銘柄まで選定する。条件が弱い場合は0銘柄も許容する。
5. 同一セクターは原則最大2銘柄までとする。
6. 各銘柄にエントリー、損切り、利確、無効化条件、リスクを具体的に設定する。
7. `reports/screening/YYYY-MM-DD.md` を作成する。
8. `history/recommendations.csv` に推薦を追記する。

## 評価手順

1. 推薦翌営業日以降の値動きを確認する。
2. 5営業日、10営業日、20営業日のリターンを確認する。
3. MFE、MAE、Target 到達、Stop 到達、先行イベントを記録する。
4. 約定可否と結果を `history/evaluations.csv` に追記する。
5. `reports/reviews/YYYY-MM-DD.md` を作成し、良い選定、悪い選定、エントリー問題、銘柄選定問題、地合い急変、セクター集中、高値掴み、材料出尽くし、需給悪化、データ精度問題を分けて整理する。

## ルール改善手順

改善はモデル学習ではなく、選定ルールと評価基準をファイル上で更新する運用である。

1. 改善案は `rules/rule_candidates.md` に記録する。
2. 原則として最低20件の約定データ、4週間以上の検証を必要とする。
3. Profit Factor、期待値、最大ドローダウン、セクター偏り、比較条件、未来情報混入の有無を確認する。
4. 採用条件を満たした案のみ `rules/current_rules.md` に反映し、バージョンを上げる。
5. 変更理由は `rules/improvement_history.md` に追記する。
6. 棄却した案は `rules/rejected_rules.md` に残す。

## Git 運用

- 重要なルール変更、推薦、評価、月次レビュー、実験結果は Git で記録する。
- コミット前に `git status --short` と差分を確認する。
- ユーザーの未コミット変更がある場合、今回の作業と無関係な変更を勝手に含めない。
- push はユーザーが明示的に依頼した場合のみ行う。ただし、週次スクリーニング自動化では `prompts/weekly_screening.md` の公開手順に従い、選定関連コミットを `main` へ push する。force push は行わない。

## 静的レポートサイト運用

- 公開対象は、推薦、事後評価、市場環境、現行ルール、改善履歴、各種レポートとする。
- `history/`、`rules/`、`reports/` の Markdown と CSV を正本とし、生成済み HTML を編集しない。
- 静的サイトは `scripts/build_site.py` で `_site/` に生成する。`_site/` は Git 管理しない。
- GitHub Pages への配信は GitHub Actions で行い、証券口座接続、注文機能、入力フォーム、利用者データ収集は追加しない。
- 株価や推薦情報には基準日を表示し、投資助言ではない旨をサイト全体で明示する。
- 公開前に、秘密情報、個人情報、API キーが生成物へ含まれていないことを確認する。
- 週次スクリーニング自動化は、GitHub Pages のデプロイ成功と公開サイト上の当日レポート表示確認までを完了条件とする。
